name: Temporary PR lock repair

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  repair-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Regenerate missing Vite plugin lock entry
        run: npm install --package-lock-only --save-dev '@vitejs/plugin-react-swc@^3.11.0' --no-audit --no-fund
      - name: Upload corrected lock
        uses: actions/upload-artifact@v4
        with:
          name: corrected-package-lock
          path: package-lock.json
          if-no-files-found: error
      - name: Clean install
        run: npm ci --no-audit --no-fund
      - name: Verify Vite plugin installed
        run: npm ls @vitejs/plugin-react-swc
      - name: Production build
        run: npm run build
